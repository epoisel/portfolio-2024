import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'portfolio-24',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
